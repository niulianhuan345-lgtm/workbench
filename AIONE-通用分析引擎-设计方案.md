# AIONE 通用分析引擎 · 设计方案

> 版本：v1.0 | 2026-06-26
> 
> 核心问题：用户可能从任意维度、问任意深度的问题。不能用一堆 Skill 逐个覆盖，需要一个通用引擎。

---

## 一、为什么不能逐问题写 Skill

万相台无界的投放层级决定了：**每个层级都会影响其他层级，用户可能从任意位置切入。**

```
账户
├── 场景 ← "关键词场景怎么样"
│   └── 计划 ← "这个计划为什么ROI掉了"
│       └── 单元
│           ├── 关键词 ← "这个词CPC怎么涨了"
│           ├── 人群   ← "拉新成本为什么翻倍"
│           └── 创意   ← "这个素材是不是疲劳了"
```

如果"用户问关键词"写一个 Skill，"用户问计划+关键词"再写一个，"用户问关键词+创意"再写一个——**组合爆炸，永远写不完。**

---

## 二、引擎核心思路：假设 → 验证 → 交叉 → 结论

不是一个规则匹配器，而是一个数据驱动的推理引擎：

```
现象
  → Step 1: 生成假设（这个维度可能的原因有哪些）
  → Step 2: 逐一验证（拉历史数据，看哪个假设成立）
  → Step 3: 交叉确认（这个原因在其他维度有没有同步表现）
  → Step 4: 输出结论
```

### 为什么是"假设-验证"而不是"规则匹配"

| 规则匹配 | 假设-验证 |
|---------|----------|
| "CPC > ¥3 → 判定为异常" | "CPC ¥4.2，可能原因：①出价改了 ②竞品抬价 ③系统选词。查上周CPC是¥4.0→排除①②，是系统选词策略的常态" |
| 规则是死的，不知道上下文 | 验证是活的，量变和质变能分开 |

---

## 三、引擎四步详细设计

### Step 1：意图解析 + 假设生成

**输入**：用户自然语言
**输出**：{ 目标维度, 目标指标, 时间范围, 对比基准, 假设列表 }

```
用户："关键词场景最近一周怎么样"

解析结果：
  目标维度：场景(关键词推广)
  目标指标：花费/ROI/CPC/CVR/转化
  时间范围：最近7天
  对比基准：上周
  假设列表：[
    "花费异常集中到某几个词？"
    "CPC整体涨了？"
    "CVR降了？"
    "零转化词变多了？"
    "不同子策略(品类词vs流量金卡)效率分化？"
  ]
```

**假设列表从哪来？** 不是从平台知识来，是从"这个维度下，影响指标的因素有哪些"来：

| 维度 | 影响因素 | 可生成假设 |
|------|---------|----------|
| 任意维度 | 时间趋势 | "是不是量变了？是不是效率变了？" |
| 任意维度 | 集中度 | "是不是少数几个东西占了大头？" |
| 任意维度 | 内部差异 | "是不是不同类型的东西效率差很多？" |
| 场景 | 子策略分布 | "是不是某个子策略拖后腿？" |
| 计划 | 预算/出价 | "是不是撞线了？是不是出价策略不对？" |
| 关键词 | 匹配方式 | "是不是广泛匹配带来了垃圾流量？" |
| 创意 | 素材周期 | "是不是素材疲劳了？" |
| 人群 | 人群类型 | "拉新和收割的效率是不是分化了？" |

### Step 2：并行取数 + 假设验证

不只是拉当前数据，还要拉**历史对比数据**。验证不是看绝对值，是看**变化量**：

```
假设："CPC整体涨了？"

验证：
  本周CPC ¥2.1 vs 上周 ¥2.0 → 涨了5%（正常波动）
  → 假设不成立，排除

假设："花费向某几个词集中？"

验证：
  TOP1词花费占比：本周 35% vs 上周 18%
  → 假设成立 ✅ 保留
```

**验证逻辑**：对比型（环比/同比） > 绝对值型。因为 ¥2.1 在一个行业正常、另一个行业异常。

### Step 3：交叉确认

数据层面最核心的一步。**不只是"这个词 CPC 涨了"，而是"这个词 CPC 涨了，它的创意 CTR 也在降，同时它的花费占比在扩大"——三条证据指向一个结论。**

```
已验证假设："流量金卡计划花费集中到一个词"
已验证假设："那个词的 CVR 在降"
已验证假设："那个词的主创意 CTR 降了 60%"

交叉确认：
  三条证据的交叉点 → 主创意疲劳 → 点击质量下降 → CVR降 → 
  但花费仍向该创意集中 → 放大损失

  根因：创意疲劳导致效率下降，预算分配机制未及时调整
  不是选词问题，不是出价问题，是创意和预算分配的问题
```

**交叉确认的价值**：单维度分析会告诉你"这个词 CVR 降了"——但不会告诉你为什么降。

### Step 4：输出结论

```
❌ 报表式："关键词场景花费¥5,046，ROI 4.36，10/18计划零转化"
✅ 诊断式：
    "关键词场景整体ROI 4.36，但内部严重分化：
     
     品类词（5个计划）— ROI 14.11，CPC仅¥0.27，表现健康
     流量金卡（2个计划）— ¥429零转化，CPC¥4.21是品类词的15倍
     好货快投词包 — ¥438零转化，同期收割模式ROI 5.57正常
     卡位 — ¥356零转化，但CPC¥3.36意图强，问题在落地页
     
     核心问题不是关键词场景不行，是流量金卡和词包两个策略
     选了错误的关键词来源。品类词、好货快投收割、控成交成本
     三个策略都正常。"
```

---

## 四、引擎实现方式

### 不是一堆 Skill，是一个引擎 + 维度配置

```
┌─────────────────────────────────────────┐
│           AIONE 通用分析引擎              │
│                                         │
│  ① 意图解析器 ← 通用（不依赖维度）         │
│  ② 假设生成器 ← 维度配置表（每个维度一套）   │
│  ③ 数据验证器 ← 通用（对比+趋势+集中度）    │
│  ④ 交叉确认器 ← 通用（多维度关联）          │
│  ⑤ 结论合成器 ← 通用（自然语言生成）        │
│                                         │
│  MCP 工具链 ← 标准化接口                  │
└─────────────────────────────────────────┘
```

### 维度配置表（不是 Skill 列表，是配置）

```json
{
  "场景": {
    "聚合维度": ["biz_code"],
    "内部差异": ["子策略分组"],
    "下钻关联": ["计划"],
    "关键指标": ["花费", "ROI", "CPC", "CVR", "零转化率"],
    "假设模板": ["花费集中度", "子策略效率分化", "环比变化"]
  },
  "计划": {
    "聚合维度": ["campaign_id"],
    "内部差异": ["出价策略", "投放时段"],
    "下钻关联": ["关键词", "创意", "人群"],
    "关键指标": ["花费", "ROI", "CPC", "CVR", "预算使用率"],
    "假设模板": ["出价策略是否匹配", "预算是否撞线", "花费是否集中"]
  },
  "关键词": {
    "聚合维度": ["keyword_title"],
    "内部差异": ["匹配方式", "词性"],
    "上卷关联": ["计划", "创意"],
    "关键指标": ["花费", "ROI", "CPC", "CVR", "花费占比"],
    "假设模板": ["CPC变化", "CVR变化", "花费集中度", "匹配方式效率"]
  },
  "创意": {
    "聚合维度": ["creative_id"],
    "内部差异": ["素材类型"],
    "上卷关联": ["计划", "关键词"],
    "关键指标": ["花费", "CTR", "CVR", "ROI", "花费占比"],
    "假设模板": ["CTR趋势(疲劳)", "CVR变化", "预算错配"]
  },
  "人群": {
    "聚合维度": ["crowd_name"],
    "内部差异": ["人群类型(拉新/认知/老客)"],
    "上卷关联": ["计划"],
    "关键指标": ["花费", "ROI", "CPA", "转化率"],
    "假设模板": ["人群类型效率分化", "CPA变化", "拉新vs收割比例"]
  }
}
```

### 实际运行

**用户问任何问题 → 引擎只做四件事：**

1. **解析意图** → 目标维度是"场景"？"计划"？还是问了一个具体计划？
2. **加载配置** → 从维度配置表找到该维度的假设模板和下钻关联
3. **执行验证** → 拉数据（当前+历史）→ 逐假设验证 → 保留成立的
4. **交叉关联** → 根据配置里的"下钻关联/上卷关联"，去关联维度找证据

**新增一个分析能力 = 新增一行维度配置，不是新写一个 Skill。**

---

## 五、和现有 Skill 的关系

| 现有 | 问题 | 改为 |
|------|------|------|
| 1.1 投放全景诊断 | 固定流程，输出固定 | → 引擎 + 场景维度配置 |
| 1.2 关键词分析 | 只能分析关键词 | → 引擎 + 关键词维度配置 |
| 1.3 场景对比 | 只能对比场景 | → 引擎自动做内部差异分析 |
| 1.4 计划排名 | 只有排名 | → 引擎自动做假设验证 |

**不需要新写 6 个 Skill，一个引擎驱动所有分析。** 现有 Skill 的 MCP 调用逻辑可以复用，但分析逻辑统一到引擎里。

---

## 六、实施优先级

| 优先级 | 内容 | 工作量 | 说明 |
|:--:|------|:--:|------|
| P0 | 引擎框架 + 场景维度 | 2天 | 替换 1.1 和 1.3，验证可行性 |
| P0 | 计划维度 + 下钻关联 | 1天 | 替换 1.4，验证交叉确认 |
| P1 | 关键词/创意/人群维度 | 2天 | 替换 1.2/1.5/1.6 |
| P1 | 交叉确认逻辑 | 1天 | 多维度关联验证 |
| P2 | 更多维度扩展 | 按需 | 维度配置表加一行即可 |

---

## 附：平台知识的作用（参考，非核心）

平台知识（从每日学习积累）在引擎中的作用仅限于：

1. **假设生成时的补充**：比如看到"流量金卡"这个策略名，知道它是一种系统选词策略，可以生成"系统选词是否匹配品类"这个假设。但这个假设跟"CPC环比变化""花费集中度"是同级的——数据验证不过就排除。
2. **结论输出时的解释**：比如结论是"建议从流量金卡切到品类词"，可以补充解释"因为流量金卡是系统选词，CPC不可控"——但这只是让解释更完整，不影响诊断逻辑本身。

**平台知识不等于因果真相。因果真相是数据验证出来的。**
